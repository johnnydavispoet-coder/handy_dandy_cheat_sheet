## All you need about installing and running a venv on Debian Linux
# The command to install the venv
python3 -m venv venv
# The command to run the venv
source venv/bin/activate

# Install after making the venv
pip install requests
pip install beautifulsoup4
pip install scapy
pip install paramiko

# When finished with the venv just type:
deactivate

