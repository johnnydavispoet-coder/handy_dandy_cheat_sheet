
grep -Ei "api[_-]?key|secret|token|jwt|bearer|authorization"

Hardcoded Values
grep -E "\"[a-zA-Z0-9]{20,}\""

API Calls
grep -E "fetch\(|axios|XMLHttpRequest"

Endpoints
grep -E "/api|/v1|/admin|/auth"

